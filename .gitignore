# Build brief (kept local — not part of the published skill)
CLAUDE.md

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Node (in case the skill grows tooling later)
node_modules/
npm-debug.log*
.env
.env.local

# SQLite transient sidecars (catalog.db itself IS committed; these are not)
*.db-wal
*.db-shm

# Generated tool outputs (theme/capture/segment artifacts written into the repo during a run)
/theme.css
/theme.json
/capture.json
/segment.json
.uiforge-scratch/
